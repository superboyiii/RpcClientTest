using Neo.Network.RPC;
using System;

namespace RpcClientTest
{
    class WalletAPI_Test
    {
        private readonly RpcClient rpcClient = new RpcClient("http://localhost:12332");
        
    }
}
